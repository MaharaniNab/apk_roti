name: apk_roti
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  d_info: ^0.2.2
  d_view: ^0.0.5
  http: ^1.2.2
  d_input: ^0.1.3
  feather_icons: ^1.2.0
  geolocator: ^13.0.1
  google_fonts: ^4.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
